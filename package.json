{
  "name": "stake-n-shares-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c green,cyan \"uvicorn backend.app.main:app --reload --port 8000\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}